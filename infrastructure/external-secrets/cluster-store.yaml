apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: azure-keyvault
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: "https://dalia-vault-1773035031.vault.azure.net/"
      serviceAccountRef:
        name: eso-azure-auth
        namespace: external-secrets
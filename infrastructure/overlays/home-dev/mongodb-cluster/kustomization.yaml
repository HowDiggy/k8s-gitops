# infrastructure/overlays/home-dev/mongodb-cluster/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/mongodb-cluster

patches:
  - path: replica-patch.yaml

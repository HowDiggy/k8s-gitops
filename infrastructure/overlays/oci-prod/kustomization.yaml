# Self-contained overlay (no paths outside this dir).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - sealed-secrets.yaml
  - cnpg-operator.yaml
  - kube-prometheus-stack.yaml


apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: logstash
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/HowDiggy/k8s-gitops.git'
    path: apps/logstash
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: elk
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  # Ensure Elasticsearch is synced first
  dependencies:
    - name: elasticsearch

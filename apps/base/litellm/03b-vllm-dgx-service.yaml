apiVersion: v1
kind: Service
metadata:
  name: vllm-dgx
  namespace: litellm
  labels:
    app.kubernetes.io/name: vllm-dgx
    app.kubernetes.io/component: inference
spec:
  type: ExternalName
  externalName: happy
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

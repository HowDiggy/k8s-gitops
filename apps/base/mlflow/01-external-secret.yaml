apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mlflow-secrets
  namespace: mlflow
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: doppler-backend
    kind: ClusterSecretStore
  target:
    name: mlflow-secrets
    template:
      type: Opaque
  data:
    - secretKey: MLFLOW_DB_PASSWORD
      remoteRef:
        key: MLFLOW_DB_PASSWORD
    - secretKey: SEAWEEDFS_S3_ACCESS_KEY
      remoteRef:
        key: SEAWEEDFS_S3_ACCESS_KEY
    - secretKey: SEAWEEDFS_S3_SECRET_KEY
      remoteRef:
        key: SEAWEEDFS_S3_SECRET_KEY
    - secretKey: MLFLOW_ADMIN_PASSWORD
      remoteRef:
        key: MLFLOW_ADMIN_PASSWORD
    - secretKey: MLFLOW_ADMIN_USER
      remoteRef:
        key: MLFLOW_ADMIN_USER
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mlflow-db-credentials
  namespace: mlflow
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: doppler-backend
    kind: ClusterSecretStore
  target:
    name: mlflow-db-credentials
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "mlflow_user"
        password: "{{ .MLFLOW_DB_PASSWORD }}"
  data:
    - secretKey: MLFLOW_DB_PASSWORD
      remoteRef:
        key: MLFLOW_DB_PASSWORD
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mlflow-auth-config
  namespace: mlflow
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: doppler-backend
    kind: ClusterSecretStore
  target:
    name: mlflow-auth-config
    template:
      type: Opaque
      data:
        basic_auth.ini: |
          [mlflow]
          default_permission = READ
          database_uri = postgresql://mlflow_user:{{ .MLFLOW_DB_PASSWORD }}@mlflow-db-rw:5432/mlflow_auth
          admin_username = {{ .MLFLOW_ADMIN_USER }}
          admin_password = {{ .MLFLOW_ADMIN_PASSWORD }}
  data:
    - secretKey: MLFLOW_DB_PASSWORD
      remoteRef:
        key: MLFLOW_DB_PASSWORD
    - secretKey: MLFLOW_ADMIN_PASSWORD
      remoteRef:
        key: MLFLOW_ADMIN_PASSWORD
    - secretKey: MLFLOW_ADMIN_USER
      remoteRef:
        key: MLFLOW_ADMIN_USER
